class Div {

    constructor(id) {
       
        this.element = document.createElement("div")
        this.element.id = id
    }
    addNaDiv(parent) {
       // this.element.appendChild(parent)
    }
}